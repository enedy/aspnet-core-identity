namespace AspnetCoreIdentity.Identity.Constants
{
    public class ClaimValues
    {
        public const string Read = nameof(Read);
        public const string Delete = nameof(Delete);
    }
}
